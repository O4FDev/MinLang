use "./math.min" as math
use "./people/person.min" as people

let ada: people.Person = people.Person {
    name: "Ada"
    age: math.answer()
}

print(people.describe(ada))
print(math.twice(ada.age))

let everyone: List<people.Person> = []
everyone.add(ada)
print(people.firstName(everyone))
