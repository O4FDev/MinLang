record Name { first: Text; last: Text }
record Person { age: Integer; name: Name; tags: List<Text> }
let person = Person {
    tags: ["compiler", "language"]
    name: Name { last: "Lovelace"; first: "Ada" }
    age: 36
}
print(person.name.first)
print(person.age)
print(person.tags[1])
