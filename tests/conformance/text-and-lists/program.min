let words = ["Min", "yar"]
let aliases = words
aliases.add("🙂")
words[0] = "M"
let joined = joinText(words)
print(joined)
print(joined.length)
print(joined.byteLength)
print(joined[4])
print(joined.slice(1, 4))
let nested: List<List<Integer>> = [[], [1, 2], []]
print(nested[1][1])
