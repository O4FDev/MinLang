function main() {
    let language = "Min" + "yar"
    print(language.length)
    print(language[0])
    print(Text(42))
    print(argumentCount())
    print(argument(0))

    let source = readTextFile(argument(1))
    print(source.length > 0)
    writeTextFile(argument(2), language)
}
